import 'package:flutter/material.dart';

class SRegisteredGroups extends StatelessWidget {
  const SRegisteredGroups({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
